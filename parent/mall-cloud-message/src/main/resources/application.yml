server:
  port: 8763
  #context-path: /message
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8781/eureka/
spring:
  application:
    name: message
  activemq:
    broker-url: tcp://localhost:61616
    in-memory: true
    pool:
      enabled: false